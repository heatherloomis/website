{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Install NPM Dependencies",
            "type": "shell",
            "command": "docker run --rm -it -v ${workspaceFolder}:/usr/src/app -w /usr/src/app node npm install",
            "problemMatcher": []
        }
    ]
}
